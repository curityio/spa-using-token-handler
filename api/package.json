{
    "name": "example-api",
    "version": "1.0.0",
    "description": "A trivial example API to validate JWTs and return data to the SPA",
    "main": "server.ts",
    "scripts": {
        "build": "rimraf dist && tsc",
        "start": "npm run build && node dist/server.js"
    },
    "author": "Curity AB",
    "license": "Apache-2.0",
    "dependencies": {
        "express": "^4.17.1",
        "express-oauth-jwt": "^2.0.1"
    },
    "devDependencies": {
        "@types/express": "^4.17.9",
        "@types/node": "^14.14.10",
        "rimraf": "^3.0.2",
        "typescript": "^4.7.2"
    }
}
