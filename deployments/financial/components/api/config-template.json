{
    "port": 3002,
    "jwksUrl": "$JWKS_ENDPOINT",
    "issuer": "$ISSUER_URI",
    "audience": "api.example.com",
    "keystoreFilePath": "$SSL_CERT_FILE_PATH",
    "keystorePassword": "$SSL_CERT_PASSWORD"
}